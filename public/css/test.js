// const { post } = require("jquery");

ClassicEditor.create( document.querySelector( '#newclass' ) )
.catch( error => {
    console.error( error );
} );


// var $log = $( "#log" ),str = '<p>sgsfg</p><p>agsk;gf</p><ul><li>agfdsg</li></ul>',html = $.parseHTML( str );
//   console.log(html)
// // Append the parsed HTML
// $log.append( html );
 
// Gather the parsed HTML's node names
